# Python dependencies for Streamlit Community Cloud.
#
# Mirrors environment.yml but pip-only.  Dev-only packages (pytest,
# black, jupyter, ipykernel) and Conda-specific packages with heavy
# native bindings that are not used by the dashboard (geopandas,
# shapely) are deliberately omitted to keep the cloud image small.
streamlit>=1.31
pandas>=2.1
numpy>=1.26
pyarrow>=15.0
scipy>=1.12
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18
scikit-learn>=1.3
reportlab>=4.0
python-docx>=1.1
